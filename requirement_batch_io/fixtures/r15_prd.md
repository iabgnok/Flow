# PRD：工作流批量测试面板 v0.2

## 模块 M1 工作流列表
- 展示 YAML 名称、最近校验结果、最近执行状态；支持按档位筛选。

## 模块 M2 一键跑批
- 选择 manifest，触发顺序执行并写 SUMMARY；失败条目记录 stderr 摘要。

## 模块 M3 通知（可选）
- 跑批结束后向 Webhook 投递摘要计数。

## 验收
- 列表 100 条内滚动流畅；跑批失败不中断后续条目（可配置）。
- 导出 CSV 含 run_id、耗时、最终状态。
